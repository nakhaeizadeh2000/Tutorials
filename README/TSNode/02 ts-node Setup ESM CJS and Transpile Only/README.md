## 0. Prerequisites and version map

- **Prereq** (see root path order, prior tracks)
- **Era** (ts-node tsx era)
- **Overlap** (see § Overlaps to avoid)

## 1. Core Concepts of ts-node Setup ESM CJS and Transpile Only

### [1.1. What ts-node Setup ESM CJS and Transpile Only Means](<./sections/1. Core Concepts of ts-node Setup ESM CJS and Transpile Only/1.1. What ts-node Setup ESM CJS and Transpile Only Means.md>)

1. **Definition** (what 1.1. What ts-node Setup ESM CJS and Transpile Only Means means in ts-node Setup ESM CJS and Transpile Only)
2. **Example** (runnable ts-node Setup ESM CJS and Transpile Only example)
3. **Pitfall** (common mistake with 1.1. What ts-node Setup ESM CJS and Transpile Only Means)

---

### [1.2. Why ts-node Setup ESM CJS and Transpile Only Matters](<./sections/1. Core Concepts of ts-node Setup ESM CJS and Transpile Only/1.2. Why ts-node Setup ESM CJS and Transpile Only Matters.md>)

1. **Definition** (what 1.2. Why ts-node Setup ESM CJS and Transpile Only Matters means in ts-node Setup ESM CJS and Transpile Only)
2. **Example** (runnable ts-node Setup ESM CJS and Transpile Only example)
3. **Pitfall** (common mistake with 1.2. Why ts-node Setup ESM CJS and Transpile Only Matters)

---

## 2. Deep Dive into ts-node Setup ESM CJS and Transpile Only

### [2.1. How ts-node Setup ESM CJS and Transpile Only Works Internally](<./sections/2. Deep Dive into ts-node Setup ESM CJS and Transpile Only/2.1. How ts-node Setup ESM CJS and Transpile Only Works Internally.md>)

1. **Definition** (what 2.1. How ts-node Setup ESM CJS and Transpile Only Works Internally means in ts-node Setup ESM CJS and Transpile Only)
2. **Example** (runnable ts-node Setup ESM CJS and Transpile Only example)
3. **Pitfall** (common mistake with 2.1. How ts-node Setup ESM CJS and Transpile Only Works Internally)

---

### [2.2. Tradeoffs and Patterns of ts-node Setup ESM CJS and Transpile Only](<./sections/2. Deep Dive into ts-node Setup ESM CJS and Transpile Only/2.2. Tradeoffs and Patterns of ts-node Setup ESM CJS and Transpile Only.md>)

1. **Definition** (what 2.2. Tradeoffs and Patterns of ts-node Setup ESM CJS and Transpile Only means in ts-node Setup ESM CJS and Transpile Only)
2. **Example** (runnable ts-node Setup ESM CJS and Transpile Only example)
3. **Pitfall** (common mistake with 2.2. Tradeoffs and Patterns of ts-node Setup ESM CJS and Transpile Only)

---

## 3. Practical ts-node Setup ESM CJS and Transpile Only in Production

### [3.1. Using ts-node Setup ESM CJS and Transpile Only with Node and TS](<./sections/3. Practical ts-node Setup ESM CJS and Transpile Only in Production/3.1. Using ts-node Setup ESM CJS and Transpile Only with Node and TS.md>)

1. **Definition** (what 3.1. Using ts-node Setup ESM CJS and Transpile Only with Node and TS means in ts-node Setup ESM CJS and Transpile Only)
2. **Example** (runnable ts-node Setup ESM CJS and Transpile Only example)
3. **Pitfall** (common mistake with 3.1. Using ts-node Setup ESM CJS and Transpile Only with Node and TS)

---

## 4. Important Points and Mentor Checklist

- **Checklist** (what senior must enforce)

---

## 5. Common Pitfalls to Production Bugs

- **Pitfall** (typical prod bug)

---

## 6. Interview Q and A

- **Q/A** (interview tradeoff)

---

## 7. Overlaps to Avoid

Links to sibling domains that already cover adjacent material.

---

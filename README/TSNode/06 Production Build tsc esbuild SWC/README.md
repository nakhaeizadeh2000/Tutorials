## 0. Prerequisites and version map

- **Prereq** (see root path order, prior tracks)
- **Era** (ts-node tsx era)
- **Overlap** (see § Overlaps to avoid)

## 1. Core Concepts of Production Build tsc esbuild SWC

### [1.1. What Production Build tsc esbuild SWC Means](<./sections/1. Core Concepts of Production Build tsc esbuild SWC/1.1. What Production Build tsc esbuild SWC Means.md>)

1. **Definition** (what 1.1. What Production Build tsc esbuild SWC Means means in Production Build tsc esbuild SWC)
2. **Example** (runnable Production Build tsc esbuild SWC example)
3. **Pitfall** (common mistake with 1.1. What Production Build tsc esbuild SWC Means)

---

### [1.2. Why Production Build tsc esbuild SWC Matters](<./sections/1. Core Concepts of Production Build tsc esbuild SWC/1.2. Why Production Build tsc esbuild SWC Matters.md>)

1. **Definition** (what 1.2. Why Production Build tsc esbuild SWC Matters means in Production Build tsc esbuild SWC)
2. **Example** (runnable Production Build tsc esbuild SWC example)
3. **Pitfall** (common mistake with 1.2. Why Production Build tsc esbuild SWC Matters)

---

## 2. Deep Dive into Production Build tsc esbuild SWC

### [2.1. How Production Build tsc esbuild SWC Works Internally](<./sections/2. Deep Dive into Production Build tsc esbuild SWC/2.1. How Production Build tsc esbuild SWC Works Internally.md>)

1. **Definition** (what 2.1. How Production Build tsc esbuild SWC Works Internally means in Production Build tsc esbuild SWC)
2. **Example** (runnable Production Build tsc esbuild SWC example)
3. **Pitfall** (common mistake with 2.1. How Production Build tsc esbuild SWC Works Internally)

---

### [2.2. Tradeoffs and Patterns of Production Build tsc esbuild SWC](<./sections/2. Deep Dive into Production Build tsc esbuild SWC/2.2. Tradeoffs and Patterns of Production Build tsc esbuild SWC.md>)

1. **Definition** (what 2.2. Tradeoffs and Patterns of Production Build tsc esbuild SWC means in Production Build tsc esbuild SWC)
2. **Example** (runnable Production Build tsc esbuild SWC example)
3. **Pitfall** (common mistake with 2.2. Tradeoffs and Patterns of Production Build tsc esbuild SWC)

---

## 3. Practical Production Build tsc esbuild SWC in Production

### [3.1. Using Production Build tsc esbuild SWC with Node and TS](<./sections/3. Practical Production Build tsc esbuild SWC in Production/3.1. Using Production Build tsc esbuild SWC with Node and TS.md>)

1. **Definition** (what 3.1. Using Production Build tsc esbuild SWC with Node and TS means in Production Build tsc esbuild SWC)
2. **Example** (runnable Production Build tsc esbuild SWC example)
3. **Pitfall** (common mistake with 3.1. Using Production Build tsc esbuild SWC with Node and TS)

---

## 4. Important Points and Mentor Checklist

- **Checklist** (what senior must enforce)

---

## 5. Common Pitfalls to Production Bugs

- **Pitfall** (typical prod bug)

---

## 6. Interview Q and A

- **Q/A** (interview tradeoff)

---

## 7. Overlaps to Avoid

Links to sibling domains that already cover adjacent material.

---

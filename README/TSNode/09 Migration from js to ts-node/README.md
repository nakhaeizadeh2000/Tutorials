## 0. Prerequisites and version map

- **Prereq** (see root path order, prior tracks)
- **Era** (ts-node tsx era)
- **Overlap** (see § Overlaps to avoid)

## 1. Core Concepts of Migration from js to ts-node

### [1.1. What Migration from js to ts-node Means](<./sections/1. Core Concepts of Migration from js to ts-node/1.1. What Migration from js to ts-node Means.md>)

1. **Definition** (what 1.1. What Migration from js to ts-node Means means in Migration from js to ts-node)
2. **Example** (runnable Migration from js to ts-node example)
3. **Pitfall** (common mistake with 1.1. What Migration from js to ts-node Means)

---

### [1.2. Why Migration from js to ts-node Matters](<./sections/1. Core Concepts of Migration from js to ts-node/1.2. Why Migration from js to ts-node Matters.md>)

1. **Definition** (what 1.2. Why Migration from js to ts-node Matters means in Migration from js to ts-node)
2. **Example** (runnable Migration from js to ts-node example)
3. **Pitfall** (common mistake with 1.2. Why Migration from js to ts-node Matters)

---

## 2. Deep Dive into Migration from js to ts-node

### [2.1. How Migration from js to ts-node Works Internally](<./sections/2. Deep Dive into Migration from js to ts-node/2.1. How Migration from js to ts-node Works Internally.md>)

1. **Definition** (what 2.1. How Migration from js to ts-node Works Internally means in Migration from js to ts-node)
2. **Example** (runnable Migration from js to ts-node example)
3. **Pitfall** (common mistake with 2.1. How Migration from js to ts-node Works Internally)

---

### [2.2. Tradeoffs and Patterns of Migration from js to ts-node](<./sections/2. Deep Dive into Migration from js to ts-node/2.2. Tradeoffs and Patterns of Migration from js to ts-node.md>)

1. **Definition** (what 2.2. Tradeoffs and Patterns of Migration from js to ts-node means in Migration from js to ts-node)
2. **Example** (runnable Migration from js to ts-node example)
3. **Pitfall** (common mistake with 2.2. Tradeoffs and Patterns of Migration from js to ts-node)

---

## 3. Practical Migration from js to ts-node in Production

### [3.1. Using Migration from js to ts-node with Node and TS](<./sections/3. Practical Migration from js to ts-node in Production/3.1. Using Migration from js to ts-node with Node and TS.md>)

1. **Definition** (what 3.1. Using Migration from js to ts-node with Node and TS means in Migration from js to ts-node)
2. **Example** (runnable Migration from js to ts-node example)
3. **Pitfall** (common mistake with 3.1. Using Migration from js to ts-node with Node and TS)

---

## 4. Important Points and Mentor Checklist

- **Checklist** (what senior must enforce)

---

## 5. Common Pitfalls to Production Bugs

- **Pitfall** (typical prod bug)

---

## 6. Interview Q and A

- **Q/A** (interview tradeoff)

---

## 7. Overlaps to Avoid

Links to sibling domains that already cover adjacent material.

---

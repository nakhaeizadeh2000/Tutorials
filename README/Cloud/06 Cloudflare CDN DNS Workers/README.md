## 0. Prerequisites and version map

- **Prereq** (see root path order, prior tracks)
- **Era** (AWS Terraform Cloudflare era)
- **Overlap** (see § Overlaps to avoid)

## 1. Core Concepts of Cloudflare CDN DNS Workers

### [1.1. What Cloudflare CDN DNS Workers Means](<./sections/1. Core Concepts of Cloudflare CDN DNS Workers/1.1. What Cloudflare CDN DNS Workers Means.md>)

1. **Definition** (what 1.1. What Cloudflare CDN DNS Workers Means means in Cloudflare CDN DNS Workers)
2. **Example** (runnable Cloudflare CDN DNS Workers example)
3. **Pitfall** (common mistake with 1.1. What Cloudflare CDN DNS Workers Means)

---

### [1.2. Why Cloudflare CDN DNS Workers Matters](<./sections/1. Core Concepts of Cloudflare CDN DNS Workers/1.2. Why Cloudflare CDN DNS Workers Matters.md>)

1. **Definition** (what 1.2. Why Cloudflare CDN DNS Workers Matters means in Cloudflare CDN DNS Workers)
2. **Example** (runnable Cloudflare CDN DNS Workers example)
3. **Pitfall** (common mistake with 1.2. Why Cloudflare CDN DNS Workers Matters)

---

## 2. Deep Dive into Cloudflare CDN DNS Workers

### [2.1. How Cloudflare CDN DNS Workers Works Internally](<./sections/2. Deep Dive into Cloudflare CDN DNS Workers/2.1. How Cloudflare CDN DNS Workers Works Internally.md>)

1. **Definition** (what 2.1. How Cloudflare CDN DNS Workers Works Internally means in Cloudflare CDN DNS Workers)
2. **Example** (runnable Cloudflare CDN DNS Workers example)
3. **Pitfall** (common mistake with 2.1. How Cloudflare CDN DNS Workers Works Internally)

---

### [2.2. Tradeoffs and Patterns of Cloudflare CDN DNS Workers](<./sections/2. Deep Dive into Cloudflare CDN DNS Workers/2.2. Tradeoffs and Patterns of Cloudflare CDN DNS Workers.md>)

1. **Definition** (what 2.2. Tradeoffs and Patterns of Cloudflare CDN DNS Workers means in Cloudflare CDN DNS Workers)
2. **Example** (runnable Cloudflare CDN DNS Workers example)
3. **Pitfall** (common mistake with 2.2. Tradeoffs and Patterns of Cloudflare CDN DNS Workers)

---

## 3. Practical Cloudflare CDN DNS Workers in Production

### [3.1. Using Cloudflare CDN DNS Workers with Node and TS](<./sections/3. Practical Cloudflare CDN DNS Workers in Production/3.1. Using Cloudflare CDN DNS Workers with Node and TS.md>)

1. **Definition** (what 3.1. Using Cloudflare CDN DNS Workers with Node and TS means in Cloudflare CDN DNS Workers)
2. **Example** (runnable Cloudflare CDN DNS Workers example)
3. **Pitfall** (common mistake with 3.1. Using Cloudflare CDN DNS Workers with Node and TS)

---

## 4. Important Points and Mentor Checklist

- **Checklist** (what senior must enforce)

---

## 5. Common Pitfalls to Production Bugs

- **Pitfall** (typical prod bug)

---

## 6. Interview Q and A

- **Q/A** (interview tradeoff)

---

## 7. Overlaps to Avoid

Links to sibling domains that already cover adjacent material.

---

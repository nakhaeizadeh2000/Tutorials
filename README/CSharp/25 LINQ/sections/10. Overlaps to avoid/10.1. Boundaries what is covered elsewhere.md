## 10.1. Boundaries: what is covered elsewhere in this repo

1. **LINQ general iterator/IEnumerable fundamentals** overlap with `README/CSharp/21 Collections` (enumeration, iterator blocks, deferred execution framing); this domain focuses on the operator behaviors you listed (ordering, element access, projection, and aggregations).
2. **Lambda/closure basics** overlap with `README/CSharp/19 Delegates and Events` and `README/CSharp/17 Extension Methods and others`; here it's only referenced where it impacts LINQ allocations and correctness.
3. **Projection into anonymous types/tuples** overlaps with `README/CSharp/23 Anonymous Types` and `README/CSharp/24 Tuples`; this domain treats projection as "shape transformation" but doesn't re-explain those specific language constructs.

---

### 1. **Collections domain**

For **`yield return`**, iterator state machines, **`IEnumerable<T>` vs `IEnumerator<T>`**, and collection types that back LINQ behavior, see **Collections** section files. This LINQ domain assumes you can enumerate a sequence and focuses on **standard query operators**.

---

### 2. **Delegates, events, extension methods**

Deep coverage of **delegate types**, **variance**, **events**, and **extension method** resolution lives under **Delegates and Events** and **Extension Methods and others**. Here we only connect those ideas to **lambda captures** and **LINQ call patterns** (see §6.3).

---

### 3. **Anonymous types and tuples**

When **`Select`** projects into **`new { ... }`** or value tuples, the **language rules** and **naming/equality** semantics belong in **Anonymous Types** and **Tuples**. In LINQ files, projections are shown only as examples.

**Anti-duplication rule:** if you expand tutorial content, add **full** explanations in the **canonical** domain above and keep **one-line pointers** here.

---

### Cross-links (relative from this file)

- Collections: `../../21 Collections/README.md`
- Delegates and Events: `../../19 Delegates and Events/README.md`
- Extension Methods and others: `../../17 Extension Methods and others/README.md`
- Anonymous Types: `../../23 Anonymous Types/README.md`
- Tuples: `../../24 Tuples/README.md`

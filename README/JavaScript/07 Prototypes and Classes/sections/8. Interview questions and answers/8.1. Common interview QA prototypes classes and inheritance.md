## 8.1. Common interview Q&A: prototypes, classes, and inheritance

### 1) **The classics with mechanism-level answers**

**Q1. What does `new C()` do step-by-step?**
A — 1) `OrdinaryCreateFromConstructor(new.target, C.prototype)` → plain object with `[[Prototype]] = C.prototype`; 2) call `C` with `this` = that object, `new.target` = `C`, `this` parameterizes delegation; 3) if constructor returns an object, that object wins; else `this`. If `C` is `class`, bare `C()` throws `TypeError` (→ [2.1](../2.%20Constructors%20and%20new/2.1.%20The%20new%20operator%20prototype%20property%20new.target%20and%20return%20override.md)).

**Q2. `__proto__` vs `.prototype` vs `[[Prototype]]`?**
A — `[[Prototype]]` is per-object internal link (`getPrototypeOf`); `.prototype` is per-function template `new` stamps (→ [2.1](../2.%20Constructors%20and%20new/2.1.%20The%20new%20operator%20prototype%20property%20new.target%20and%20return%20override.md)); `__proto__` is legacy accessor on `Object.prototype` reflecting `[[Prototype]]` (slow mutation path → [1.2](../1.%20Prototype%20delegation/1.2.%20Creation%20mutation%20and%20performance%20costs.md)). Plain objects have no `.prototype`.

**Q3. Why does `d instanceof C` become `false` after `C.prototype = {}`?**
A — `instanceof` checks *current* `C.prototype` in `d`'s chain, not birth prototype. `d` holds old `C.prototype` link; after reassignment old not found → `false`. `Object.getPrototypeOf(d) !== C.prototype` then (→ [2.1](../2.%20Constructors%20and%20new/2.1.%20The%20new%20operator%20prototype%20property%20new.target%20and%20return%20override.md) / [5.1](../5.%20Type%20checks%20and%20reflection/5.1.%20instanceof%20isPrototypeOf%20Symbol.hasInstance%20and%20cross-realm%20failures.md)).

**Q4. Where does assignment `obj.x = 1` put the value when a prototype accessor `set x` exists?**
A — not own `x`; calls prototype setter with `this` = `obj` (see delegation shadowing). If prototype has data `writable: false`, strict `obj.x = 1` throws `TypeError` (sloppy silent no-op) — no shadow created; `defineProperty` would still create own (→ [1.1](../1.%20Prototype%20delegation/1.1.%20Delegation%20mechanics%20lookup%20shadowing%20and%20dynamic%20updates.md)).

**Q5. `class` hoisted like `function`?**
A — no; TDZ like `let`. `new C()` before `class C {}` throws `ReferenceError` (→ [3.1](../3.%20Class%20syntax/3.1.%20Class%20basics%20declarations%20hoisting%20methods%20accessors%20and%20fields.md)). Function declarations hoist (→ [Variables §2.2](../../../04%20Variables%20Scope%20and%20Closures/sections/2.%20Scope%20and%20lookup/2.2.%20Hoisting%20the%20reality%20creation%20phase%20not%20magic.md)).

**Q6. What does `super()` vs `super.method()` do?**
A — `super()` in derived constructor calls parent constructor allocating `this` (must be before touching `this` — ReferenceError otherwise). `super.method()` inside a method follows `[[HomeObject]].[[Prototype]]` lexically, not `this`'s chain (→ [3.2](../3.%20Class%20syntax/3.2.%20Extends%20and%20super%20chain%20wiring%20and%20obligations.md)).

**Q7. Private `#x` vs `_x`?**
A — `#x` is per-instance truly private, non-enumerable/serializable, brand-checked (`#x in obj` tests brand); `_x` is conventional string key, enumerable, mutable from anywhere (→ [3.3](../3.%20Class%20syntax/3.3.%20Private%20fields%20and%20methods%20brand%20checks%20and%20encapsulation.md)). Each class's `#x` is distinct even with same spelling; subclass `#x` is new slot, not inheritance.

**Q8. Static `#count` shared with subclass?**
A — no; static private is per-class brand. `class Parent { static #c=0; static inc(){this.#c++} }` `Child.inc()` with `this===Child` fails brand (TypeError) because `Child` lacks `Parent`'s static `#c` brand (→ [3.4](../3.%20Class%20syntax/3.4.%20Static%20members%20static%20blocks%20and%20class%20field%20order.md)).

**Q9. Why does `arr instanceof Array` fail across iframes?**
A — each realm has its own `Array` and `Array.prototype`; `arr` chain holds `realmA.Array.prototype`, not `realmB.Array.prototype` → walk misses. Use `Array.isArray(arr)` (realm-safe) or `Array.prototype.isPrototypeOf` still realm-fragile (→ [5.1](../5.%20Type%20checks%20and%20reflection/5.1.%20instanceof%20isPrototypeOf%20Symbol.hasInstance%20and%20cross-realm%20failures.md)).

**Q10. How does `Symbol.hasInstance` customize `instanceof`?**
A — `static [Symbol.hasInstance](v)` on the right-hand constructor overrides `instanceof`; `value instanceof C` calls `C[Symbol.hasInstance](value)`. `isPrototypeOf` ignores the hook (→ [5.1](../5.%20Type%20checks%20and%20reflection/5.1.%20instanceof%20isPrototypeOf%20Symbol.hasInstance%20and%20cross-realm%20failures.md)).

**Q11. Instance field `x = 1` on prototype?**
A — no; own per instance, enumerable. Prototype method `method()` is shared, non-enumerable (→ [3.1](../3.%20Class%20syntax/3.1.%20Class%20basics%20declarations%20hoisting%20methods%20accessors%20and%20fields.md)). Field `method = () => {}` shadows same-named prototype method (silent bug).

**Q12. `Object.assign(Proto, trait)` vs descriptor copy?**
A — `assign` reads trait getters (getter fires on Trait, value flattened) and writes data; descriptor copy `defineProperties(Target.prototype, getOwnPropertyDescriptors(trait))` preserves accessors and enumerability (→ [4.1](../4.%20Composition%20and%20mixins/4.1.%20Mixins%20and%20composition%20patterns.md)).

### 2) **What interviewers actually probe for**

- Tracing chain walks: `d instanceof C` after `C.prototype` replacement or `Object.setPrototypeOf`; they expect "current `C.prototype` not birth".
- `[[HomeObject]]` vs receiver: `super.method()` not following `this.__proto__` but method's home.
- Brand vs `instanceof`: `#field in obj` vs `instanceof` after prototype mutation; they expect brand persists while `instanceof` breaks.
- Class TDZ and `new` enforcement vs function hoisting.

### 3) **Grading guidance**

| Signal | Junior | Mid | Senior |
|--------|--------|-----|--------|
| Enumerates `new` steps and `prototype` vs `getPrototypeOf` | ✓ | ✓ | ✓ |
| Explains shadowing vs setter call and why `Object.assign` flattens accessors | — | ✓ | ✓ |
| Traces `extends` wiring two chains and `super()` pre-`this` TDZ | — | ✓ | ✓ |
| Contrasts private `in` brand vs `instanceof` cross-realm vs `Symbol.hasInstance` | — | — | ✓ |

Red flags: "`__proto__` is the prototype template", "`class` hoists", "`instanceof` tests birth class", "private `#x` is inherited", "`static` fields on prototype".

**Cross-links:** delegation mechanics → [1.1](../1.%20Prototype%20delegation/1.1.%20Delegation%20mechanics%20lookup%20shadowing%20and%20dynamic%20updates.md); prototype creation → [1.2](../1.%20Prototype%20delegation/1.2.%20Creation%20mutation%20and%20performance%20costs.md); constructor/`new` → [2.1](../2.%20Constructors%20and%20new/2.1.%20The%20new%20operator%20prototype%20property%20new.target%20and%20return%20override.md); class basics → [3.1](../3.%20Class%20syntax/3.1.%20Class%20basics%20declarations%20hoisting%20methods%20accessors%20and%20fields.md); super/extends → [3.2](../3.%20Class%20syntax/3.2.%20Extends%20and%20super%20chain%20wiring%20and%20obligations.md); private containment → [3.3](../3.%20Class%20syntax/3.3.%20Private%20fields%20and%20methods%20brand%20checks%20and%20encapsulation.md); static order → [3.4](../3.%20Class%20syntax/3.4.%20Static%20members%20static%20blocks%20and%20class%20field%20order.md); `instanceof` realm → [5.1](../5.%20Type%20checks%20and%20reflection/5.1.%20instanceof%20isPrototypeOf%20Symbol.hasInstance%20and%20cross-realm%20failures.md).

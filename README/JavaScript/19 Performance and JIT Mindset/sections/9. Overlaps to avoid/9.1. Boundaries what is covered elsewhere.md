## 9.1. Boundaries what is covered elsewhere

1. **Scope/hoisting/TDZ/closure capture mechanics and whole-environment retention — [Variables, Scope, and Closures](<../../../04 Variables Scope and Closures/README.md>); block-function Annex B nuances — there (here only the IC/throughput angle of retained shape).**
2. **SameValueZero/`Map`/`Set` ordering and general collection selection — [Collections Map Set and Weak References](<../../../09 Collections Map Set and Weak References/README.md>); heap/stack roots, generational/incremental GC, `WeakMap`/`WeakSet` ephemeron, `WeakRef`/`FinalizationRegistry` best-effort — [Memory Management and GC](<../../../18 Memory Management and GC/README.md>) (here only allocation-throughput and IC cost, not lifetime correctness).**
3. **Iterator protocol, `for...of` vs `for...in` semantics, generators/`yield`/`yield*`, async iteration/`for await...of` — [Iterables Generators and Async Iteration](<../../../10 Iterables Generators and Async Iteration/README.md>) (here only the per-iteration IC and yield-to-loop cost).**
4. **`Array` exotic `length`/holes/sparse vs dense semantics, copy-era `toSorted`/`toSpliced` family, sort stability and `TypedArray`/`ArrayBuffer`/`DataView` — [Arrays](<../../../08 Arrays/README.md>) (here only element-kind and IC cost).**
5. **Strings UTF-16/grapheme segmentation, `Intl.Segmenter`, template literals/tagged templates, `RegExp` flags/lastIndex/groups — [Strings and Regular Expressions](<../../../11 Strings and Regular Expressions/README.md>) (here only rope vs `join` concat cost).**
6. **Numbers IEEE-754/epsilon/`BigInt`, `Math`/`Date`/`Temporal`/`Intl.NumberFormat` — [Numbers Dates Math and Temporal](<../../../12 Numbers Dates Math and Temporal/README.md>) (here only numeric IC/kind cost).**
7. **Error classes/`try/catch/finally`, `cause`/`AggregateError`, `stack`/`prepareStackTrace`, `console`/`inspector` workflow — [Error Handling and Debugging](<../../../15 Error Handling and Debugging/README.md>) (here only the flame/profile lens).**
8. **`Proxy` handler traps/invariants, `Reflect` forwarding and `receiver`, membranes/revocables/`@@species` — [Metaprogramming Symbols Proxy Reflect](<../../../17 Metaprogramming Symbols Proxy Reflect/README.md>) (here only the trap overhead vs `Object.freeze` invariant).**
9. **JSON `stringify`/`parse`/`toJSON`/`structuredClone` vs `JSON` lossiness and data-exchange patterns — [JSON Serialization and Data Exchange](<../../../16 JSON Serialization and Data Exchange/README.md>) (here only stringify/parse throughput, not format correctness).**
10. **Modules ESM vs CJS/live bindings, dynamic `import()`/`import.meta`, `package.json` `exports`/`importmap` — [Modules and Code Organization](<../../../14 Modules and Code Organization/README.md>) (here only parse/parse-slice and chunk load cost, not bundler semantics).**
11. **Event-loop queues, microtask vs macrotask, `Promise` states/combinators/`async`/`await` desugaring and `AbortSignal` wiring — [Async Event Loop and Promises](<../../../13 Async Event Loop and Promises/README.md>) (here only yielding/bounded-concurrency throughput).**

---

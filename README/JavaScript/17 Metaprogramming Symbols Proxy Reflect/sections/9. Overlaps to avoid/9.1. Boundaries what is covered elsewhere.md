## 9.1. Boundaries what is covered elsewhere

1. **Primitive vs object, `Symbol` as type, `BigInt`, boxing and `Object(sym)` wrapping** — the type-level `typeof sym === "symbol"`, wrapper truthiness, `new Symbol()` throws, and `ToPrimitive` fallback `valueOf`/`toString` live in [Values Types and Coercion](<../../../03 Values Types and Coercion/README.md>) ([Eight types](<../../../03 Values Types and Coercion/sections/1. The value model/1.1. Eight types two families primitives vs objects.md>), [ToPrimitive dance](<../../../03 Values Types and Coercion/sections/2. The conversion algorithms/2.3. ToPrimitive and the valueOf toString and Symbol.toPrimitive dance.md>), [Wrapper vs primitive](<../../../03 Values Types and Coercion/sections/4. Boxing and wrapper objects/4.1. Wrapper objects vs primitives boxing demystified.md>)). This domain only covers *well-known symbol hooks* plus registry/hidden-key mechanics (see [1.2](<../1. Symbols deep/1.2. Symbol creation description global registry and hidden keys.md>)) — not `Symbol` as a value type.

2. **Descriptors, `defineProperty`, enumeration order, `Reflect.ownKeys`, `preventExtensions`/`seal`/`freeze`, `Object.assign`/`keys`/`entries`/`hasOwn`** — owned by [Objects in Depth](<../../../06 Objects in Depth/README.md>) ([Descriptors](<../../../06 Objects in Depth/sections/2. Properties and descriptors/2.1. Data vs accessor descriptors and attribute defaults.md>), [Defining](<../../../06 Objects in Depth/sections/2. Properties and descriptors/2.2. Defining observing and configuring properties.md>), [Enumerability](<../../../06 Objects in Depth/sections/2. Properties and descriptors/2.3. Enumerability enumeration order and for-in mechanics.md>)). This domain only adds the *proxy invariants* that constrain those operations (see [2.2](<../2. Proxy core/2.2. Proxy invariants non-configurable non-extensible and exotic constraints.md>)).

3. **Prototype chain, constructors/`new`/`new.target`, `class`/`extends`/`super`, `#private` fields, brand checks, `static {}` order, `instanceof` ordinary walk and cross-realm** — owned by [Prototypes and Classes](<../../../07 Prototypes and Classes/README.md>) ([Delegation](<../../../07 Prototypes and Classes/sections/1. Prototype delegation/1.1. Delegation mechanics lookup shadowing and dynamic updates.md>), [Class basics](<../../../07 Prototypes and Classes/sections/3. Class syntax/3.1. Class basics declarations hoisting methods accessors and fields.md>), [Private](<../../../07 Prototypes and Classes/sections/3. Class syntax/3.3. Private fields and methods brand checks and encapsulation.md>), [Type checks](<../../../07 Prototypes and Classes/sections/5. Type checks and reflection/5.1. instanceof isPrototypeOf Symbol.hasInstance and cross-realm failures.md>)). This domain only customizes `instanceof` via `@@hasInstance` and subclassing via `@@species` (see [4.1](<../4. Metaprogramming customization/4.1. Customizing coercion toPrimitive toStringTag hasInstance and match hooks.md>) and [4.2](<../4. Metaprogramming customization/4.2. Species unscopables isConcatSpreadable and subclassing built-ins.md>)).

4. **Array length/holes/sort, `Array.isArray` cross-realm, `isConcatSpreadable` use at call-site** — owned by [Arrays](<../../../08 Arrays/README.md>) ([Exotic](<../../../08 Arrays/sections/1. Arrays are exotic objects/1.1. What arrays really are the indexed exotic length and Array.isArray.md>), [Concat spreadable](<../../../08 Arrays/sections/4. Searching slicing and joining/4.2. Slicing concatenating and joining slice concat join at with.md>)); `Set`/`Map`/`WeakMap`/`WeakSet`/`WeakRef`/`FinalizationRegistry` — [Collections](<../../../09 Collections Map Set and Weak References/README.md>). This domain only defines `@@isConcatSpreadable` and `@@species` as hook contracts — per-method flattening/sort stability stays there.

5. **Iterables/`for...of`/`Array.from`, generator `function*`/`yield`, `Symbol.iterator`/`Symbol.asyncIterator`, `for await...of`** — owned by [Iterables Generators and Async Iteration](<../../../10 Iterables Generators and Async Iteration/README.md>) ([Iterable vs iterator](<../../../10 Iterables Generators and Async Iteration/sections/1. Iterable and iterator protocols/1.1. Iterable vs iterator vs array-like and Symbol.iterator.md>), [Async iterables](<../../../10 Iterables Generators and Async Iteration/sections/4. Async iteration/4.1. Async iterables Symbol.asyncIterator and for await...of.md>)). This domain owns `Symbol.iterator` as a hook name only (see [1.1](<../1. Symbols deep/1.1. Well-known symbols the protocol hook table.md>)) — consumption/closing/return stays there.

6. **RegExp flags/groups and `@@match` dispatch use at call-site** — owned by [Strings and Regular Expressions](<../../../11 Strings and Regular Expressions/README.md>) ([RegExp creation](<../../../11 Strings and Regular Expressions/sections/4. RegExp essentials/4.1. RegExp creation literals vs constructor flags and lastIndex state.md>), [Groups/hasIndices](<../../../11 Strings and Regular Expressions/sections/5. Advanced RegExp/5.1. Groups named groups backreferences hasIndices and replacement patterns.md>)); **`JSON`/`structuredClone` vs symbols** (`JSON.stringify` skips symbols, `structuredClone` drops them) — [JSON Serialization and Data Exchange](<../../../16 JSON Serialization and Data Exchange/README.md>) ([What stringify drops](<../../../16 JSON Serialization and Data Exchange/sections/2. Stringifying and serialization/2.1. What JSON.stringify serializes and what it drops.md>)); **Error taxonomy/`cause`/`AggregateError` and `Error.stack`** — [Error Handling and Debugging](<../../../15 Error Handling and Debugging/README.md>).

7. **Modules `import`/`export` live bindings and `Proxy` as capability boundaries for host code** — owned by [Modules and Code Organization](<../../../14 Modules and Code Organization/README.md>); **event-loop microtask ordering for `Promise` capability proxies and async stacks** — [Async Event Loop and Promises](<../../../13 Async Event Loop and Promises/README.md>); **descriptors/GC/performance and `WeakRef` internals** — Memory/Memory Management domain (when it lands) and [Values 2.2](<../../../03 Values Types and Coercion/sections/2. The conversion algorithms/2.2. ToString and ToNumber the exact conversion tables.md>).

---



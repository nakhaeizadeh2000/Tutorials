## 9.1. Boundaries what is covered elsewhere

1. **Scope mechanics, hoisting, TDZ, and per-iteration closure capture** — [Variables, Scope, and Closures](<../../../04 Variables Scope and Closures/README.md>) (this domain covers only what a closed-over graph *retains* and when it dies, not how lexical environments are created or layered — see there for [closures](<../../../04 Variables Scope and Closures/sections/3. Closures/3.1. Closures functions that remember their birth environment.md>) ↔ [lifetime §3.1](<../3. Lifetimes what stays alive/3.1. Closures timers listeners maps and the lifetime of a graph.md>)).
2. **SameValueZero/`Map`/`Set` ordering/performance, `Map` vs Object trade-offs, and `WeakRef`/`FinalizationRegistry` *use* contract** — [Collections Map Set and Weak References](<../../../09 Collections Map Set and Weak References/README.md>) (there owns membership/iteration contracts; here owns the heap/GC-engine view of why ephemeron entries/weak slots behave that way — see [ephemeron §4.1](<../4. Weak references and cleanup deep dive/4.1. WeakMap and WeakSet ephemeron semantics revisited deep.md>) vs [Collections §3.1](<../../../09 Collections Map Set and Weak References/sections/3. Weak collections/3.1. WeakMap ephemeron keys non-iterability and use cases.md>)).
3. **Event-loop phasing, micro vs macrotask starvation, `setTimeout`/`queueMicrotask`/`nextTick` ordering** — [Async Event Loop and Promises](<../../../13 Async Event Loop and Promises/README.md>) (here only the GC-root implication: host queues/registries as retaining roots and `AbortSignal`-scoped teardown — see [reachability §1.2](<../1. Memory model/1.2. Reachability roots retaining paths and object death.md>) ↔ [Async §1.2](<../../../13 Async Event Loop and Promises/sections/1. Event loop and the job queue/1.2. Microtasks vs macrotasks queueMicrotask and starvation.md>)).
4. **`Array` holes vs `undefined`, length invariants/`at`/`with` mutating vs copy-era, and `ArrayBuffer`/`TypedArray`/`DataView` transfer semantics** — [Arrays](<../../../08 Arrays/README.md>) (here only the allocation/retaining-pressure angle of holes/buffers/vectors — see [holes §2.1](<../../../08 Arrays/sections/2. Holes sparsity and length/2.1. Holes vs undefined sparse arrays and empty slots.md>) / [buffers §5.3](<../../../08 Arrays/sections/5. Transforming ordering and typed preview/5.3. ArrayBuffer TypedArray and DataView how they differ from Array.md>) ↔ [§2.2](<../2. GC engine and allocation/2.2. Allocation hidden classes inline caches and GC pressure.md>)).
5. **`JSON.stringify`/`parse` vs `structuredClone` vs `Buffer`/`base64` data-exchange patterns** — [JSON Serialization and Data Exchange](<../../../16 JSON Serialization and Data Exchange/README.md>) (here only the allocation/cloning pressure vs lossiness trade — see [structuredClone §5.1](<../../../16 JSON Serialization and Data Exchange/sections/5. Beyond JSON structuredClone and data exchange/5.1. structuredClone deep cloning and limitations.md>) ↔ [allocation §2.2](<../2. GC engine and allocation/2.2. Allocation hidden classes inline caches and GC pressure.md>)).
6. **Object descriptors, enumerability order, `preventExtensions`/`seal`/`freeze` shallow reality, `Object.assign`/`keys`/`entries`** — [Objects in Depth](<../../../06 Objects in Depth/README.md>); **prototype chain, constructors/`new`/`new.target`, `class`/`extends`/`super`, `#private` brand, `static {}` order** — [Prototypes and Classes](<../../../07 Prototypes and Classes/README.md>); **`Proxy` handler traps / invariants / `Reflect.*` receiver forwarding / membranes / `Proxy.revocable`** — [Metaprogramming Symbols Proxy Reflect](<../../../17 Metaprogramming Symbols Proxy Reflect/README.md>) (here only the invariant/GC-interaction: why sealed targets constrain proxy lies and where membranes add GC-friendly indirection).
7. **`Intl` number/date formatting, `Temporal`/`Date` parsing & arithmetic, `Math`/`BigInt` precision, and `RegExp` backtracking memory** — [Numbers Dates Math and Temporal](<../../../12 Numbers Dates Math and Temporal/README.md>) / [Strings and Regular Expressions](<../../../11 Strings and Regular Expressions/README.md>) (here only how those graphs allocate and when they promote to old space — see [heap §1.1](<../1. Memory model/1.1. Heap stack execution contexts and references how values live.md>) ↔ [Arrays buffers](<../../../08 Arrays/sections/5. Transforming ordering and typed preview/5.3. ArrayBuffer TypedArray and DataView how they differ from Array.md>)).
8. **Module singletons and `import`/`export` live bindings** — [Modules and Code Organization](<../../../14 Modules and Code Organization/README.md>) (module-exported `Map` as a GC root is the only overlap — here ownership/policy, not import resolution).
9. **Stack traces, `console`/`inspector` debugging workflow, `Error` cause trees, `using`/`Symbol.dispose` statement mechanics** — [Error Handling and Debugging](<../../../15 Error Handling and Debugging/README.md>) (here only the `heapProfiler`/`--heapsnapshot-signal`/`--expose-gc` instrumentation and `using` as deterministic correctness — see [measuring §5.1](<../5. Measuring and preventing leaks/5.1. Measuring memory heap snapshots allocation timeline and inspector.md>) ↔ [Error Handling §5.3](<../../../15 Error Handling and Debugging/sections/5. Debugging toolkit/5.3. Source maps Node inspector DevTools workflow and flags.md>)).

**Stop-list:** do not expand the above neighbors into this domain. Every concept above gets a one-line mention plus link; the full treatment lives in its owner domain.

---

## 8.1. Boundaries what is covered elsewhere

1. **Arrays' exotic `length`, holes, and specific methods** — Arrays domain owns indexed exotic invariants, hole vs `undefined`, `length` auto-bump/truncation, `copyWithin`/`fill`, `toReversed`/`toSorted`/`toSpliced`/`with`, `sort` stability, and `TypedArray`/`ArrayBuffer`. Here we use arrays only as the canonical iterable consumer (`for-of`/spread/`Array.from`) — array method contracts live in [Arrays](<../../../08 Arrays/README.md>) ([1.1](<../../../08 Arrays/sections/1. Arrays are exotic objects/1.1. What arrays really are the indexed exotic length and Array.isArray.md>), [2.1](<../../../08 Arrays/sections/2. Holes sparsity and length/2.1. Holes vs undefined sparse arrays and empty slots.md>), [3.2](<../../../08 Arrays/sections/3. Mutation and its copy-era alternative/3.2. Non-mutating copies toReversed toSorted toSpliced and with ES2023.md>), [5.3](<../../../08 Arrays/sections/5. Transforming ordering and typed preview/5.3. ArrayBuffer TypedArray and DataView how they differ from Array.md>)).

2. **Map/Set/WeakMap/WeakSet/WeakRef/FinalizationRegistry and SameValueZero** — Collections domain owns ordered insertion semantics, SameValueZero discipline, ephemeron lifetime, `WeakRef`/`FinalizationRegistry` non-determinism. Here we use `Map`/`Set` only as iterable sources (`entries`/`keys`/`values`) and to illustrate iteration order; collection choice and GC pressure live in [Collections](<../../../09 Collections Map Set and Weak References/README.md>) ([1.1](<../../../09 Collections Map Set and Weak References/sections/1. Map deep dive/1.1. Map creation SameValueZero insertion order and iteration.md>), [3.1](<../../../09 Collections Map Set and Weak References/sections/3. Weak collections/3.1. WeakMap ephemeron keys non-iterability and use cases.md>), [4.1](<../../../09 Collections Map Set and Weak References/sections/4. Weak references and cleanup/4.1. WeakRef semantics deref and lifetime reality.md>)).

3. **Object descriptors, enumeration order (`for-in`, `Object.keys`), and prototype/class** — Objects in Depth owns data vs accessor descriptors, attribute defaults, and enumeration order rules; Prototypes and Classes owns `class`/`extends`/`super`/`private`/`#` brand. Here we reference enumeration vs iteration contrast only to correct `for-in` misuse; descriptor/`__proto__`/`class` iteration helpers live in [Objects in Depth](<../../../06 Objects in Depth/README.md>) ([2.3](<../../../06 Objects in Depth/sections/2. Properties and descriptors/2.3. Enumerability enumeration order and for-in mechanics.md>), [5.1](<../../../06 Objects in Depth/sections/5. Integrity and immutability/5.1. Prevent extensions seal freeze and shallow immutability.md>)) and [Prototypes and Classes](<../../../07 Prototypes and Classes/README.md>).

4. **Function forms, `this` parameter, closures/GC, and full promise mechanics** — Functions Deep Dive owns `this` binding/`call`/`apply`/`bind`/arrow lexical `this`; Variables/Scope owns `var`/`let`/`const`/TDZ/scope chain/closure capture; Language Basics owns `for-in` vs `for-of` control-flow. Fully asynchronous coordination beyond `for await`/`async function*` (Promise states, microtask queue, event loop, `Promise.all`/`any`/`race`, `queueMicrotask`) lives in [Async Event Loop and Promises](<../../../13 Async Event Loop and Promises/README.md>) ([1.1](<../../../13 Async Event Loop and Promises/sections/1. Event loop and the job queue/1.1. Call stack run-to-completion and the event loop.md>), [3.1](<../../../13 Async Event Loop and Promises/sections/3. Promises core/3.1. Promise states executor thenable assimilation and the job queue.md>), [4.1](<../../../13 Async Event Loop and Promises/sections/4. Promise combinators and utilities/4.1. Promise.all and allSettled parallel aggregation.md>)) — here async iteration only *consumes* `Promise`-wrapped `next()`.

5. **Strings/regex internals, Numbers/Dates/Temporal/Math, JSON, and module/registry side effects** — String code unit vs point vs grapheme deep dive, `Intl.Segmenter`, `RegExp` `exec` iteration, `Number`/`Math`/`BigInt`/`Date`/`Temporal` formatting, `JSON.stringify` replacer/reviver contracts, `import`/`export`/live bindings, and deterministic disposal (`using`/`Symbol.dispose` superseding manual `return()`) live in [Strings (11)](<../../../11 Strings and Regular Expressions/README.md>), Numbers/Dates (12), JSON (16), Modules (14), and Memory/Error Handling (18/15) domains — here strings appear only as iterable examples.

6. **Iterators that keep resources alive indefinitely or framework streaming APIs** — DOM `NodeList` liveness, Node `ReadableStream`/`AsyncIterable` concrete bindings, and framework RX pipelines live in host/platform tracks — here we model resources abstractly (cursors/queues) and defer host specifics.

**Stop-list (this domain does NOT re-teach):**
- [ ] Array method tables (`push`/`pop`/`splice`/`sort`) — Arrays domain.
- [ ] `Map`/`Set` SameValueZero tables and `WeakMap` ephemeron correctness — Collections domain.
- [ ] Object descriptor/attribute semantics and prototype chain — Objects / Prototypes domains.
- [ ] Full promise combinators/microtask ordering/event-loop scheduling — [Async Event Loop and Promises](<../../../13 Async Event Loop and Promises/README.md>).
- [ ] `Temporal`/`Intl`/`RegExp` iteration and `JSON` serialization — Numbers / Strings / JSON domains (11/12/16).

**Cross-links:** use this domain for iterable/iterator protocol, `for-of`/`for await...of`, spread/`Array.from`/destructuring, generators (`function*`/`yield`/`yield*`/`next`/`throw`/`return`), and async generators; link to neighbors instead of re-stating their material — that is the single-source-of-truth contract (§5 Anti-boilerplate).

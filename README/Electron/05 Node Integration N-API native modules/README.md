## 0. Prerequisites and version map

- **Prereq** (see root path order, prior tracks)
- **Era** (Electron 34 era)
- **Overlap** (see § Overlaps to avoid)

## 1. Core Concepts of Node Integration N-API native modules

### [1.1. What Node Integration N-API native modules Means](<./sections/1. Core Concepts of Node Integration N-API native modules/1.1. What Node Integration N-API native modules Means.md>)

1. **Definition** (what 1.1. What Node Integration N-API native modules Means means in Node Integration N-API native modules)
2. **Example** (runnable Node Integration N-API native modules example)
3. **Pitfall** (common mistake with 1.1. What Node Integration N-API native modules Means)

---

### [1.2. Why Node Integration N-API native modules Matters](<./sections/1. Core Concepts of Node Integration N-API native modules/1.2. Why Node Integration N-API native modules Matters.md>)

1. **Definition** (what 1.2. Why Node Integration N-API native modules Matters means in Node Integration N-API native modules)
2. **Example** (runnable Node Integration N-API native modules example)
3. **Pitfall** (common mistake with 1.2. Why Node Integration N-API native modules Matters)

---

## 2. Deep Dive into Node Integration N-API native modules

### [2.1. How Node Integration N-API native modules Works Internally](<./sections/2. Deep Dive into Node Integration N-API native modules/2.1. How Node Integration N-API native modules Works Internally.md>)

1. **Definition** (what 2.1. How Node Integration N-API native modules Works Internally means in Node Integration N-API native modules)
2. **Example** (runnable Node Integration N-API native modules example)
3. **Pitfall** (common mistake with 2.1. How Node Integration N-API native modules Works Internally)

---

### [2.2. Tradeoffs and Patterns of Node Integration N-API native modules](<./sections/2. Deep Dive into Node Integration N-API native modules/2.2. Tradeoffs and Patterns of Node Integration N-API native modules.md>)

1. **Definition** (what 2.2. Tradeoffs and Patterns of Node Integration N-API native modules means in Node Integration N-API native modules)
2. **Example** (runnable Node Integration N-API native modules example)
3. **Pitfall** (common mistake with 2.2. Tradeoffs and Patterns of Node Integration N-API native modules)

---

## 3. Practical Node Integration N-API native modules in Production

### [3.1. Using Node Integration N-API native modules with Node and TS](<./sections/3. Practical Node Integration N-API native modules in Production/3.1. Using Node Integration N-API native modules with Node and TS.md>)

1. **Definition** (what 3.1. Using Node Integration N-API native modules with Node and TS means in Node Integration N-API native modules)
2. **Example** (runnable Node Integration N-API native modules example)
3. **Pitfall** (common mistake with 3.1. Using Node Integration N-API native modules with Node and TS)

---

## 4. Important Points and Mentor Checklist

- **Checklist** (what senior must enforce)

---

## 5. Common Pitfalls to Production Bugs

- **Pitfall** (typical prod bug)

---

## 6. Interview Q and A

- **Q/A** (interview tradeoff)

---

## 7. Overlaps to Avoid

Links to sibling domains that already cover adjacent material.

---

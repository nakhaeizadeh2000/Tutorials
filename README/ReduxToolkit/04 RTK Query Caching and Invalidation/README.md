## 0. Prerequisites and version map

- **Prereq** (see root path order, prior tracks)
- **Era** (RTK 2 + RTK Query era)
- **Overlap** (see § Overlaps to avoid)

## 1. Core Concepts of RTK Query Caching and Invalidation

### [1.1. What RTK Query Caching and Invalidation Means](<./sections/1. Core Concepts of RTK Query Caching and Invalidation/1.1. What RTK Query Caching and Invalidation Means.md>)

1. **Definition** (what 1.1. What RTK Query Caching and Invalidation Means means in RTK Query Caching and Invalidation)
2. **Example** (runnable RTK Query Caching and Invalidation example)
3. **Pitfall** (common mistake with 1.1. What RTK Query Caching and Invalidation Means)

---

### [1.2. Why RTK Query Caching and Invalidation Matters](<./sections/1. Core Concepts of RTK Query Caching and Invalidation/1.2. Why RTK Query Caching and Invalidation Matters.md>)

1. **Definition** (what 1.2. Why RTK Query Caching and Invalidation Matters means in RTK Query Caching and Invalidation)
2. **Example** (runnable RTK Query Caching and Invalidation example)
3. **Pitfall** (common mistake with 1.2. Why RTK Query Caching and Invalidation Matters)

---

## 2. Deep Dive into RTK Query Caching and Invalidation

### [2.1. How RTK Query Caching and Invalidation Works Internally](<./sections/2. Deep Dive into RTK Query Caching and Invalidation/2.1. How RTK Query Caching and Invalidation Works Internally.md>)

1. **Definition** (what 2.1. How RTK Query Caching and Invalidation Works Internally means in RTK Query Caching and Invalidation)
2. **Example** (runnable RTK Query Caching and Invalidation example)
3. **Pitfall** (common mistake with 2.1. How RTK Query Caching and Invalidation Works Internally)

---

### [2.2. Tradeoffs and Patterns of RTK Query Caching and Invalidation](<./sections/2. Deep Dive into RTK Query Caching and Invalidation/2.2. Tradeoffs and Patterns of RTK Query Caching and Invalidation.md>)

1. **Definition** (what 2.2. Tradeoffs and Patterns of RTK Query Caching and Invalidation means in RTK Query Caching and Invalidation)
2. **Example** (runnable RTK Query Caching and Invalidation example)
3. **Pitfall** (common mistake with 2.2. Tradeoffs and Patterns of RTK Query Caching and Invalidation)

---

## 3. Practical RTK Query Caching and Invalidation in Production

### [3.1. Using RTK Query Caching and Invalidation with Node and TS](<./sections/3. Practical RTK Query Caching and Invalidation in Production/3.1. Using RTK Query Caching and Invalidation with Node and TS.md>)

1. **Definition** (what 3.1. Using RTK Query Caching and Invalidation with Node and TS means in RTK Query Caching and Invalidation)
2. **Example** (runnable RTK Query Caching and Invalidation example)
3. **Pitfall** (common mistake with 3.1. Using RTK Query Caching and Invalidation with Node and TS)

---

## 4. Important Points and Mentor Checklist

- **Checklist** (what senior must enforce)

---

## 5. Common Pitfalls to Production Bugs

- **Pitfall** (typical prod bug)

---

## 6. Interview Q and A

- **Q/A** (interview tradeoff)

---

## 7. Overlaps to Avoid

Links to sibling domains that already cover adjacent material.

---

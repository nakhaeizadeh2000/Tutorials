## 0. Prerequisites and version map

- **Prereq** (see root path order, prior tracks)
- **Era** (Nginx 1.26 era)
- **Overlap** (see § Overlaps to avoid)

## 1. Core Concepts of Throttling limit_req limit_conn

### [1.1. What Throttling limit_req limit_conn Means](<./sections/1. Core Concepts of Throttling limit_req limit_conn/1.1. What Throttling limit_req limit_conn Means.md>)

1. **Definition** (what 1.1. What Throttling limit_req limit_conn Means means in Throttling limit_req limit_conn)
2. **Example** (runnable Throttling limit_req limit_conn example)
3. **Pitfall** (common mistake with 1.1. What Throttling limit_req limit_conn Means)

---

### [1.2. Why Throttling limit_req limit_conn Matters](<./sections/1. Core Concepts of Throttling limit_req limit_conn/1.2. Why Throttling limit_req limit_conn Matters.md>)

1. **Definition** (what 1.2. Why Throttling limit_req limit_conn Matters means in Throttling limit_req limit_conn)
2. **Example** (runnable Throttling limit_req limit_conn example)
3. **Pitfall** (common mistake with 1.2. Why Throttling limit_req limit_conn Matters)

---

## 2. Deep Dive into Throttling limit_req limit_conn

### [2.1. How Throttling limit_req limit_conn Works Internally](<./sections/2. Deep Dive into Throttling limit_req limit_conn/2.1. How Throttling limit_req limit_conn Works Internally.md>)

1. **Definition** (what 2.1. How Throttling limit_req limit_conn Works Internally means in Throttling limit_req limit_conn)
2. **Example** (runnable Throttling limit_req limit_conn example)
3. **Pitfall** (common mistake with 2.1. How Throttling limit_req limit_conn Works Internally)

---

### [2.2. Tradeoffs and Patterns of Throttling limit_req limit_conn](<./sections/2. Deep Dive into Throttling limit_req limit_conn/2.2. Tradeoffs and Patterns of Throttling limit_req limit_conn.md>)

1. **Definition** (what 2.2. Tradeoffs and Patterns of Throttling limit_req limit_conn means in Throttling limit_req limit_conn)
2. **Example** (runnable Throttling limit_req limit_conn example)
3. **Pitfall** (common mistake with 2.2. Tradeoffs and Patterns of Throttling limit_req limit_conn)

---

## 3. Practical Throttling limit_req limit_conn in Production

### [3.1. Using Throttling limit_req limit_conn with Node and TS](<./sections/3. Practical Throttling limit_req limit_conn in Production/3.1. Using Throttling limit_req limit_conn with Node and TS.md>)

1. **Definition** (what 3.1. Using Throttling limit_req limit_conn with Node and TS means in Throttling limit_req limit_conn)
2. **Example** (runnable Throttling limit_req limit_conn example)
3. **Pitfall** (common mistake with 3.1. Using Throttling limit_req limit_conn with Node and TS)

---

## 4. Important Points and Mentor Checklist

- **Checklist** (what senior must enforce)

---

## 5. Common Pitfalls to Production Bugs

- **Pitfall** (typical prod bug)

---

## 6. Interview Q and A

- **Q/A** (interview tradeoff)

---

## 7. Overlaps to Avoid

Links to sibling domains that already cover adjacent material.

---
